import 'package:flutter/material.dart';

import 'edit_page_note_body.dart';

class Pageeditnote extends StatelessWidget {
  const Pageeditnote({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body:Editnotebody() ,);
  }
}